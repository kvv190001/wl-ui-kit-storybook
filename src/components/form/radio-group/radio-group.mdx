import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as RadioGroupStories from './radio-group.stories.tsx';

<Meta of={RadioGroupStories} />

# Radio Group

The **Radio Group** allows users to select exactly one option from a set.  
Each radio button supports dark mode and is grouped by a shared `name` attribute.

<Primary />

## Controls

<Controls />

## Stories

### Default
A simple radio group.
<Story of={RadioGroupStories.Default} />
