import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as CheckboxStories from './checkbox.stories.tsx';

<Meta of={CheckboxStories} />

# Checkbox

The **Checkbox** component allows users to select one or more options from a list.  
It uses the `accent-color` property to adapt to dark mode automatically.

<Primary />

## Controls

<Controls />

## Stories

### Default
A standard checkbox.
<Story of={CheckboxStories.Default} />

