import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as SelectStories from './select.stories.tsx';

<Meta of={SelectStories} />

# Select

The **Select** component lets users choose a single value from a list of options.  
It’s styled for both light and dark modes and supports a disabled state.

<Primary />

## Controls

<Controls />

## Stories

### Default
A basic dropdown menu.
<Story of={SelectStories.Default} />

