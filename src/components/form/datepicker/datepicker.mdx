import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';
import * as DatePickerStories from './datepicker.stories.tsx';

<Meta of={DatePickerStories} />

# DatePicker

The **DatePicker** component provides a native date selection interface.  
It automatically adapts to the user’s operating system style and supports dark mode and disabled states.

<Primary />

## Controls

<Controls />

## Stories

### Default
A standard date picker input.
<Story of={DatePickerStories.Default} />
